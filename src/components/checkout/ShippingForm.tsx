// src/components/checkout/ShippingForm.tsx
// Shipping address form for the checkout page.
// Collects customer name, address, postcode, city, and country.
// Validates input before allowing payment to proceed.

"use client";

export default function ShippingForm() {
  // TODO: Build controlled form with validation (react-hook-form or native)
  // TODO: Auto-complete postcode lookup (optional integrated with a postcode API)
  return (
    <form>
      <h2>Shipping Address</h2>
      {/* Full name */}
      {/* Address line 1 & 2 */}
      {/* City */}
      {/* Postcode */}
      {/* Country */}
      <p>ShippingForm (placeholder)</p>
    </form>
  );
}
